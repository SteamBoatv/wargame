# 第三时代设计与素材台账

> 状态：持续维护
> 最后更新：2026-07-28
> 作用：第三时代玩法、素材与验收结论的唯一事实来源。后续新增想法、素材缺口和决策都写在这里，其他文档只链接本文件。历史上下文见 [本轮交接文档](handoffs/HANDOFF_era3-tank-preview_2026-07-28.md)。

## 状态标记

- `已定`：产品方向已经确认，可以直接实现。
- `临时`：当前版本采用的占位方案或首轮平衡数值，必须经过实战再定。
- `待办`：已确认需要，但本轮不阻塞核心流程。
- `假设`：尚未经过浏览器或实战验证。

## 已定设计

### 第三时代总则

- `已定` 第三时代提供限量英雄单位，不替换时代 II 的常规兵种。
- `已定` 英雄采用“同时最多存活 1 个”的限制；英雄死亡后可以重新生产，不采用“每局限购次数”。
- `已定` 掠夺军阀不生产英雄。其第三时代质变玩法另行设计。
- `已定` 游戏保持完全自动化；单位不可被玩家选中，也不接受玩家移动或攻击命令。
- `已定` 工程师的时代 III 近战英雄采用 CraftPix 绿色坦克，定位为短射程喷射、缓慢推进、笨重且结实的自动战斗单位。
- `已定` 当前不设置专门克制该坦克的角色；仍可通过普通伤害将其击毁。

### 工程师英雄状态链

```text
生产进度 100%
  → 主基地门口空投
  → 落地与启动演出
  → 缓慢自动推进
  → 短射程喷射战斗
  → 战斗结束后检查血量
      ├─ 损伤不足阈值：继续推进
      └─ 损伤达到阈值：停车自我维修 → 继续推进
```

- `已定` 空投和启动都发生在生产进度达到 100% 之后；演出不占用生产时间。
- `已定` 本轮只做本地演出，不做全局横幅、播报、镜头拉取或小地图强提示。
- `已定` 自我维修只在一次战斗结束、准备继续前进前触发。
- `已定` 单次维修最多恢复最大生命值的 10%，已经接近满血时只补实际缺口。
- `已定` 完整恢复 10% 必须是较长演出，维修主循环可以反复播放，以突出笨重感。
- `已定` 维修过程中如果重新接敌或受击，不瞬间跳帧；完成当前循环并收拢机构后恢复战斗。已经恢复的生命保留，未完成的额度作废。

## 当前首轮实现参数

以下均为 `临时` 数值，只代表可验收的 v0.1，不代表最终平衡：

| 项目 | v0.1 |
| --- | ---: |
| 名称 | 工程喷射坦克 |
| 费用 | 650 |
| 生命 | 1250 |
| 单次喷射伤害 | 44，近距小范围多目标 |
| 攻击间隔 | 1.8 秒 |
| 射程 | 84 |
| 移速 | 25 |
| 生产时间 | 12 秒 |
| 同时存活上限 | 1 |
| 空投时间 | 2.8 秒 |
| 落地启动 | 2.0 秒 |
| 维修触发阈值 | 损失至少 4% 最大生命 |
| 单次维修上限 | 10% 最大生命 |
| 维修脉冲 | 每轮 2%，最多 5 轮 |
| 单轮主循环 | 1.25 秒 |
| 展开 / 收拢 | 各 0.8 秒 |
| 维修冷却 | 9 秒 |
| 战斗结束静默判定 | 1.5 秒无交火 |

时代 III 的首轮进化门槛也属于 `临时`：

- 累计经验达到 600（时代 II 起点为 300，相当于再获取 300）。
- 金币消耗 800。

## 动作映射

| 游戏阶段 | 当前素材动作 | 播放方式 | 状态 |
| --- | --- | --- | --- |
| 待机 | `Idle.png` | 6 帧循环 | 已定 |
| 缓慢推进 | `Drive.png` | 6 帧循环，禁止额外上下弹跳 | 已定 |
| 落地启动 | `Turn.png` | 启动阶段播放 | 临时 |
| 喷射起势 | `Gas_Start.png` | 8 帧单次 | 已定 |
| 持续喷射 | `Gas_Sycle.png` | 8 帧循环 | 已定 |
| 喷射结束 | `Gas_End.png` | 8 帧单次 | 已定 |
| 维修展开 | `MoveOut.png` | 4 帧单次 | 已定 |
| 维修主循环 | `Special.png` | 6 帧，每个治疗脉冲一轮 | 已定 |
| 维修收拢 | `MoveIn.png` | 4 帧单次 | 已定 |
| 受击 | `Hurt.png` | 2 帧短动作 | 待办：首轮暂由受击闪烁表达 |
| 被摧毁 | `Death.png` | 4 帧单次 | 已定 |

## 素材来源与使用约束

### 已采用

- 来源：[CraftPix - Free Factory Boss Enemies Asset Pack for Cyberpunk](https://craftpix.net/freebies/free-factory-boss-enemies-asset-pack-for-cyberpunk/)
- 原始下载归档：`D:\Downloads\craftpix-net-412866-free-factory-boss-enemies-asset-pack-for-cyberpunk.zip`
- 原始包 SHA-256：`152CA5161340877A8F1CEBD0CC22881486104E200A216CB180D533E928C7E950`
- 工作区审阅副本：`../_era3_preview/craftpix_factory_boss_110055/`
- 正式运行时只纳入实际使用的 PNG 动作，不提交 ZIP、PSD 或未使用的整包文件。
- `已定` 允许在游戏中商用和修改，禁止把原始素材单独转售或重新分发；来源页还明确禁止把素材用于 AI 训练或 AI 生成流程。因此这些 CraftPix 图片只进入传统裁切、打包和运行时渲染，不发送给生成式工具。

### 仍需补齐

| 缺失素材 | 当前占位 | 最终验收标准 | 状态 |
| --- | --- | --- | --- |
| 像素降落伞、吊索、载具吊具 | Canvas 像素块绘制 | 与 96×96 坦克像素密度一致；下降时轮廓稳定；落地前自然脱离 | 待办 |
| 落地尘土与冲击 | 方块尘粒、缩放地面阴影 | 一眼看出重量；不遮挡血条与单位主体 | 待办 |
| 维修绿色加号 | Canvas `+` 粒子 | 小尺寸仍可读；不能与治疗兵飘字混淆 | 待办 |
| 维修焊接火花 / 工具 | 方块火花和绿色轮廓光 | 能表达“机械维修”而非魔法治疗 | 待办 |
| 空投、落地、液压展开、焊接音效 | 现有合成音临时组合 | 节奏与动作帧同步；低频突出重量 | 待办 |
| 队伍配色版本 | 绿色本体 + 敌我地环 | 双方同选工程师时仍能瞬间区分 | 待办 |
| 全局生产完成提示 | 本轮不实现 | 横幅、声音、小地图或镜头提示需另做产品决策 | 待办 |

## 后续设计待办

- `待办` 为王国元帅确定时代 III 英雄；在此之前时代 III 继续使用时代 II 常规阵容。
- `待办` 为掠夺军阀确定无英雄的时代 III 质变：掠夺质变、全军狂暴、总攻号令三选一或重新设计。
- `待办` 决定是否加入工程喷射坦克的专门反制单位；当前没有硬克制。
- `待办` 通过实战校准英雄费用、生命、范围伤害、生产时间、进化门槛、维修阈值、维修量与冷却。
- `待办` 评估 `Idle2/3`、`Drive2/3`、`Gas2/3` 是否代表可用的阶段变化、炮口变化或受损状态；本轮不混用，避免动作语义不清。
- `待办` 评估维修是否允许被修士治疗叠加，以及两种恢复同时发生时的视觉优先级。
- `待办` 设计坦克的全局生产完成提示；本轮按产品决定不做。
- `待办` 补充移动启动、停车和转向的更重型节奏；首轮先用低移速与无弹跳表达重量。
- `待办` 确认英雄是否参与老兵军衔系统。首轮建议不晋升，避免英雄光效与老兵光环冲突。

## 全局战场安全视区

- `已定` 顶部状态栏以下、底部生产栏以上才算相机的有效战场；Canvas 仍铺满全屏，HUD 不改变地图和模拟坐标。
- `已定` 相机居中、纵向拖动边界、缩放锚点和小地图视框统一按有效战场计算，避免上下城堡或单位被 HUD 遮挡。
- `已定` 生产栏会随指挥官和时代改变宽高，因此安全视区必须读取实际布局，不维护易失效的固定像素值。
- `已定` 镜头在地图顶部保留 80、底部保留 150 世界单位的可视余量；上下地面纹理同步延展，底部用于完整承托建在路线终点的己方城堡与基地圆台，不改变寻路和战斗长度。
- `已验证` 桌面与 390×844 移动视口的顶部/底部极限镜头均能完整显示对应城堡；底部城堡和基地圆台停在生产栏上方，坦克测试页同步生效。

## 验收清单

- `假设` 生产条到 100% 后才出现空投，不提前生成可战斗实体。
- `假设` 空投和启动期间不可攻击、不可被攻击，也不阻挡其他单位。
- `已验证` 启动结束后无需玩家输入便自动缓慢推进。
- `已验证` 喷射会按 Start → Cycle → End 选择动作；车体保持独立待机循环，毒雾作为炮口前方的独立特效随朝向翻转，主体底部锚点未见明显抖动。
- `已验证` 战斗结束且损伤达到阈值时停车维修；擦伤阈值仍需更多实战样本确认是否合适。
- `已验证` 满额维修从 1037/1250 恢复到 1162/1250，恰好为最大生命的 10%；展开、5 轮脉冲和收拢合计约 7.85 秒。
- `已验证` 维修中重新接敌后能结束当前循环、收拢并恢复自动推进，没有卡死或瞬移。
- `假设` 联机主客机能看到一致的空投、攻击与维修阶段，且不同协议版本不会进入同一房间。
- `已验证` 桌面与 390×844 移动视口均无页面或单位栏横向溢出，集成场景没有游戏控制台报错；移动端实机触控仍待验。

完成浏览器实测后，应将对应条目从 `假设` 改为 `已验证`，并记录失败现象与修正方式。

### 2026-07-28 浏览器集成验收

- 验收页：`../_aitest/era3/engineer_tank_flow.html`
- 默认参数按真实速度循环演示完整流程；`?fast=1` 用于快速状态机回归。
- `?fast=1&freeze=6` 可定格喷射阶段，`?fast=1&interrupt=1` 可回归维修中断。
- `?mobile=1` 使用 390×844 固定验收视口；`?camera=top` / `?camera=bottom` 分别固定在地图纵向极限，用于检查城堡与 HUD 遮挡。
- 已覆盖：空投、落地启动、自动推进、喷射、战后满额维修、维修中断、存活上限按钮禁用、桌面布局、390×844 布局和控制台错误检查。
- 2026-07-28 补充覆盖：动态 HUD 安全视区、地图上下可视余量、上下城堡完整显示、底部生产栏遮挡回归；未发现游戏脚本报错。
- 2026-07-28 图层回归：空投初段可同时看到伞、吊索与车体；验收页等待全部坦克图集载入后再开始演出；攻击时毒雾不再替换车体。
- 未覆盖：真实双端 P2P 的阶段同步、弱网快照插值、实机触控、实际生产队列从 99% 跨到 100% 的视觉瞬间。

## 决策记录

- 2026-07-28：第三时代改为限量英雄，不做第三套更贵常规兵。
- 2026-07-28：掠夺军阀不生产英雄。
- 2026-07-28：放弃继续把 AI 生成的 v5 作为工程师当前主方案，改用成熟素材库中的绿色 CraftPix 坦克。
- 2026-07-28：工程师坦克确定为短射程喷射、慢速、厚重、战后自修的自动英雄。
- 2026-07-28：维修单次最多恢复 10%，完整维修使用多轮循环并显著拉长。
- 2026-07-28：空投与启动期间不接受玩家指令，因为本游戏没有单位手操。
- 2026-07-28：全局完成提示延期，本轮允许程序化临时视觉占位。
- 2026-07-28：镜头改用 HUD 之间的动态安全视区，解决地图上下边界和生产栏遮挡战场的问题。
